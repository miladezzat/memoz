export const uuidConstants = {
  // Pattern for validating UUIDs (version 4)
  UUID_PATTERN: /^[A-F\d]{8}-[A-F\d]{4}-4[A-F\d]{3}-[89AB][A-F\d]{3}-[A-F\d]{12}-[A-Za-z0-9]{9}$/i,
};

export default uuidConstants;
